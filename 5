s = [0, 1, 1]
n = int(input('Введите номер числа фиббоначи начиная с 1: '))
if n <= len(s):
  print(s[n - 1])
else:
  for i in range(n):
    try:
      n = s[i]
    except:
      s.append(s[i - 1] + s[i - 2])
  print(s[-1])
